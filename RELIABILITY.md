# 多上级对接与工控机运维经验

依据：用户提供的《VLStream双上级国标级联问题复盘与运维说明-20260912》《VLStream无人值守恢复验收-20260913》《VLStream 系统使用说明》PDF；2026-09-15 对当前仓库源码进行核对。

平台支持对接多个上级，每个上级独立配置和验证。本文以现场联调记录沉淀经验；“双上级”描述的是案例涉及的数量，不是产品能力上限。

## 1. 经验的适用范围与能力边界

可靠性是项目与部署共同实现的。程序负责保存业务数据、注册续期和恢复会话；操作系统负责服务编排与巡检；硬件负责来电后开机。只改部署无法修复应用的数据保存缺口，只改代码也不能让未开机的工控机自动运行。

现场文档对应单独的补丁 JAR、六服务 Compose、systemd 单元、网络规则和恢复脚本。本文不会把该现场的通过结果当作当前仓库默认部署的保证，也不公开现场账号、密码、具体设备清单或网络拓扑地址。

## 2. 当前能力与归属

| 场景 | 当前源码/模板 | 部署与验收要求 |
| --- | --- | --- |
| 多上级注册、心跳、共享关系 | 已有平台表、共享关系表和注册/心跳逻辑 | 每套上级单独授权、配置与验证 |
| 离线上级重注册 | 已有 `PlatformServiceImpl.offline` 中启用平台的重试任务 | 验证对端下线/上线后恢复，不用整栈重启代替重注册 |
| 注册有效期比配置短 | 本次补充成功响应有效期解析；匹配本地 Contact 的 expires 优先，其次全局 Expires，缺失时用配置；按配置与授权有效期较小值的 80% 安排续期 | 例如配置 3600、应答 300，约 240 秒续期；每次成功后重排任务，不改数据库配置。无有效租约时不标记在线。需与实际上级联调 |
| 独立 SIP 认证用户名 | 本次修复挑战认证仍使用设备国标编号的问题，使 Authorization 与摘要计算使用同一配置用户名，空值回退设备编号 | 403 还可能来自密码、域、授权等，不能认定所有现场 403 都由此引起 |
| 同源通道向不同上级发布不同共享编号 | 已有 `PlatformChannelMapper.custom_device_id` 映射以及目录/查询取值 | 页面原始编号和共享编号不同；同一上级必须唯一，不能与平台节点重号。已有数据需检查，不自动重编号 |
| 工作台布局掉电保存 | 本次新增 `sys_work_layout`，LONGTEXT 保存完整布局，沿用全局共享布局语义；读取时兼容导入旧缓存，数据库已有记录时不覆盖 | 部署后执行 V1_2_7 迁移，首次读取并确认迁移完成，再考虑改变 Redis 模式 |
| 国标位置可靠入库 | 本次单条/批量上报直接在 Spring 事务内保存历史和通道位置；两处批量 Mapper 参数显式命名 | 旧版本位置队列先排空/迁移。直接写库会增加上报线程等待，需按现场上报量验证数据库负载 |
| 进程退出恢复 | 默认 Compose 五个服务已有 `restart: unless-stopped` 和健康检查 | Docker 必须运行；手动停止、持续不健康的语义与退出不同 |
| 系统开机自动编排 | 本次提供 `vlstream-stack.service` 新部署模板，依赖健康后启动，失败重试 | Linux 上安装启用、核对路径与 Docker 开机设置；不覆盖现有现场同名单元 |
| 持续不健康恢复与冷却 | 本次提供五服务巡检模板，连续三次不健康后定向恢复，300 秒冷却 | 是容器健康级别，不包含现场媒体 API 专项探针；六服务或外部依赖需适配 |
| Redis 重启后重建 | 巡检模板记录容器启动代次，检测重启后安排媒体和后端重启，复用项目启动恢复流程 | 需安装模板并建立观察基线；并非 Java 进程自带的完整 Redis 丢失感知。必须验证实际会话、设备及视频恢复 |
| Redis 纯缓存/tmpfs | 默认 Compose **仍启用 AOF 并挂载 redis-data**，本次不切换 | 先审计其他模块 Redis 用途、迁移布局/队列、完成状态重建和回滚验证；不得直接照抄现场切换结果 |
| 数据库落盘与备份 | 业务配置由 MySQL 持久保存；现场每日备份脚本未随原始资料提供，仓库不宣称已有该完整功能 | 部署端核对刷盘参数，设置 03:20 / 保留 7 份或实际策略，包含配置与补丁，隔离恢复验证 |
| 固定出口与媒体端口 | Compose 提供端口范围配置；默认公开映射以 UDP 为主 | TCP 模式需额外核对映射；固定 NAT、RTCP 余量、远端媒体网段由现场网络部署完成 |
| 内核 panic 与 watchdog | 不属于应用 JVM 的能力 | 操作系统配置 panic 重启；硬件 watchdog 要确认设备实际支持 |
| 来电自动开机 | 依赖 BIOS/硬件 | AC Power Recovery / Restore on AC Power Loss 设置并实测 |
| 随时断电后保证恢复 | **不能给出绝对保证** | 供电、存储损坏、网络和外部设备故障超出本机软件保证范围 |

代码定位：

- [平台注册服务](ruoyi-wvp/src/main/java/com/ruoyi/wvp/gb28181/service/impl/PlatformServiceImpl.java)、[响应有效期解析](ruoyi-wvp/src/main/java/com/ruoyi/wvp/gb28181/utils/RegistrationExpiry.java)。
- [SIP 认证请求](ruoyi-wvp/src/main/java/com/ruoyi/wvp/gb28181/transmit/cmd/SIPRequestHeaderPlarformProvider.java)。
- [工作台持久保存](ruoyi-system/src/main/java/com/ruoyi/system/service/impl/SysWorkService.java)、[布局迁移](ruoyi-admin/src/main/resources/db/migration/V1_2_7__persist_workbench_layout.sql)。
- [位置事务保存](ruoyi-wvp/src/main/java/com/ruoyi/wvp/service/impl/MobilePositionServiceImpl.java)。
- [共享映射](ruoyi-wvp/src/main/java/com/ruoyi/wvp/mapper/PlatformChannelMapper.java)、[默认 Compose](compose.yaml)。
- [新部署恢复模板及启用步骤](deploy/reliability/README.md)。

## 3. 可复用的多上级对接经验

1. **接入与注册**：本平台向每个上级分别注册。401 挑战后成功 200 是正常流程；403 要核对同一事务里的身份、域、认证用户名、密码和授权。
2. **目录与授权**：上级收到的是为它配置的共享编号。平台编号、设备编号、原始通道编号与共享编号应明确区分；上级白名单要包含实际共享视频编号。
3. **信令与媒体**：上级信令服务器和媒体服务器可能不同 IP。SIP 可达不能替代 RTP/RTCP 可达；按 SDP、UDP/TCP 模式和实际出口抓包确认。
4. **点播与解码**：404 优先检查共享映射；486/超时查源设备回流。有 RTP、发送成功甚至有 RTCP 接收报告，也要验证对端解码与画面。
5. **持续运行**：成功响应的租约可能短于本地配置；各上级独立配置，分别观察续期后是否继续有画面。

现场布控球原 H.264 对其中一套上级有流无画面，改 H.265 后恢复，是特定组合的兼容结论。不能据此宣布所有 H.264 不支持。单兵同号与回流超时也是不同问题，需要分别验证。

配置教程和虚构参数见 [国标接入与多上级示例文档](ruoyi-ui-vue3/src/assets/docs/gb28181-guide.md)，也可从国标服务信息和新增/编辑平台弹窗中的帮助入口查看、下载。

## 4. 现场记录能证明什么

2026-09-12 记录：用户确认两套上级当时全部设备可见视频。

2026-09-13 记录：完成隔离故障测试、数据库/缓存恢复、生产 Redis 故障恢复、本地 11 路解码及正常系统重启。固定配置指纹不包含在线状态、流会话、心跳时间等运行字段。

该日仍待现场完成：

- 来电自动开机（当时仍需按键开机）。
- 真实物理断电后的冷启动。
- 两套上级重新上线后的注册、心跳、目录、点播及停止验收。

9 月 12 日的双上级实播通过，不等同于 9 月 13 日无人值守调整后再次完成双上级恢复验收。

## 5. 新部署验收表

| 验收 | 记录证据 | 通过标准 |
| --- | --- | --- |
| 初始配置 | 设备、平台、共享关系及端口基线 | 与批准配置一致 |
| 布局迁移 | 数据库布局与页面保存/读取 | 旧缓存不覆盖新布局，长文本完整 |
| 数据保存 | 单条/批量位置与失败回滚 | 提交一致，异常无部分业务写入 |
| 服务异常 | 单服务退出和持续不健康、冷却日志 | 自动恢复有边界，不反复重启 |
| 缓存重启 | Redis 新启动代次、媒体/后端重建日志 | 设备、目录和实际视频恢复 |
| 正常重启 | systemd、Compose、视频解码记录 | 无人工启动服务即可恢复 |
| 定时巡检 | 至少连续三轮时间和下一次触发时间 | 持续周期执行 |
| 备份恢复 | 完整快照、恢复日志 | 隔离数据库可恢复，业务一致 |
| 多上级级联 | 各上级的 SIP 事务、共享目录、实际点播/停止和续期 | 所有本次对接的上级分别通过 |
| 断电冷启动 | 供电恢复到系统/视频恢复的现场记录 | 无需按键，系统与视频恢复 |

故障排查先读日志、查健康和会话，不自动用旧备份覆盖业务数据库。配置、队列或卷的迁移必须保留回滚材料。

文档提到的通道名称 Mapper 错误、预置位响应空值异常没有提供对应补丁源码或完整报文，需用当前接口和设备响应单独回归；不把这些日志异常当作已确认的视频根因。

## 6. 持续沉淀经验的记录模板

每次对接新增一条脱敏记录，按问题现象、协议或设备型号检索。已有经验在新环境中复验后补充适用范围；未经验证的推测保留为待确认项。

| 内容 | 记录要求 |
| --- | --- |
| 场景 | 日期、软件与设备版本、上级类型、编码和传输模式 |
| 问题现象 | 触发条件、预期表现、实际表现、影响范围 |
| 排查证据 | 脱敏 SIP 事务、共享目录、媒体流与解码结果 |
| 处理方法 | 修改前后对照、操作顺序和回滚办法 |
| 复验结果 | 每个相关上级的注册、目录、点播、停止和续期验证 |
| 适用范围 | 已验证组合、未验证条件、仍待排查的问题 |

例如“特定布控球切换 H.265 后恢复”可用于提示编码兼容排查；“300 秒有效期下提前续注册”可用于提示核对实际有效期。两者都不能直接成为所有设备、所有上级的固定参数。

## 7. 技术依据

- [SIP RFC 3261 注册与绑定刷新](https://www.rfc-editor.org/rfc/rfc3261.html#section-10.2.4)：续期要依据注册应答有效期。
- [Docker 重启策略](https://docs.docker.com/engine/containers/start-containers-automatically/)：退出恢复、手动停止和 Docker 启动行为应分别理解。

以上是源码能力与部署设计说明。测试证据与现场结论应分开登记，首页静态帮助不显示未经探测的“本机已启用/验收通过”状态。
